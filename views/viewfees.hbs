<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Student Fees</title>
    <!-- Add Bootstrap CSS link -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
    <div class="container mt-5">
        <form action="/viewfees1">
            <h3>Search Standard</h3>
            <hr>
            <div class="form-group">
                <label for="student_unique_no">Student unique no:</label>
                <input type="number" class="form-control" id="student_unique_no" name="student_unique_no" required>
            </div>
            <button type="submit" class="btn btn-primary">SEARCH Student Fees</button>
            <a href="/" class="btn btn-secondary">BACK TO HOME</a>

            {{#if mesg2}}
                <div class="container mt-3">
                    <h3>Data not found</h3>
                </div>
            {{/if}}
        </form>
    </div>

    {{#if mesg1}}
        <div class="container mt-5">
            <h3>View Student Fees</h3>
            <hr>
            <table class="table table-bordered">
                <thead class="thead-dark">
                    <tr>
                        <th>Student Unique No</th>
                        <th>Payment Amount</th>
                        <th>Fee Type</th>
                        <th>Academic Year</th>
                        <th>Payment Status</th>
                    </tr>
                </thead>
                <tbody>
                    {{#data}}
                        <tr>
                            <td>{{student_unique_no}}</td>
                            <td>{{payment_amount}}</td>
                            <td>{{fee_type}}</td>
                            <td>{{academic_year}}</td>
                            <td>{{payment_status}}</td>
                        </tr>
                    {{/data}}
                </tbody>
            </table>
            <a href="/" class="btn btn-secondary">BACK TO HOME</a>
        </div>
    {{/if}}

    <!-- Add Bootstrap JS and Popper.js if needed -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
