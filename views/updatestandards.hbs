<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search and Update Standard</title>
    <!-- Add Bootstrap CSS link -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
    <div class="container mt-5">
        <form action="/updatestandardsearch">
            <h3>Search Standard</h3>
            <hr>

            <div class="form-group">
                <label for="name">Name of Standard:</label>
                <input type="text" class="form-control" id="name" name="name" placeholder="e.g. 10th standard" required>
            </div>

            <button type="submit" class="btn btn-primary">SEARCH Standard</button>
            <a href="/" class="btn btn-secondary">BACK TO HOME</a>

            {{#if mesg2}}
                <div class="container mt-3">
                    <h3>Data not found</h3>
                </div>
            {{/if}}
            {{#if umesg}}
                <div class="container mt-3">
                    <h3>Data Updated</h3>
                </div>
            {{/if}}
        </form>
    </div>

    {{#if mesg1}}
        <div class="container mt-5">
            <form action="/updatestandard1">
                <h3>Update Standard</h3>
                <hr>
                {{#data}}
                    <div class="form-group">
                        <label for="update_name">Name of Standard:</label>
                        <input type="text" class="form-control" id="update_name" name="name" value="{{name}}" placeholder="e.g. 10th standard" required>
                    </div>

                    <div class="form-group">
                        <label for="teacher">Teacher:</label>
                        <input type="text" class="form-control" id="teacher" name="teacher" value="{{teacher}}" required>
                    </div>

                    <div class="form-group">
                        <label for="division">Division:</label>
                        <input type="text" class="form-control" id="division" name="division" value="{{division}}" required>
                    </div>

                    <div class="form-group">
                        <label for="update_max_students">Max Students:</label>
                        <input type="number" class="form-control" id="update_max_students" name="max_students" value="{{max_students}}" required>
                    </div>
                {{/data}}

                <button type="submit" class="btn btn-primary">Update Standard</button>
                <a href="/" class="btn btn-secondary">Back to Home</a>
            </form>
        </div>
    {{/if}}

    <!-- Add Bootstrap JS and Popper.js if needed -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
